# 进度与验证状态

最后更新：2026-09-09。仿真器已上线：https://7dul2.github.io/breadboard-studio/ 。环境：macOS 26.6 (arm64)、Node 26.0.0、pnpm 11.25.0、Chromium 153（Playwright 1.63）。

## 里程碑

| 阶段 | 状态 | 验证 |
| --- | --- | --- |
| M0 独立项目与最小运行环境 | 完成 | `pnpm install && pnpm dev` 启动空工作台；README 命令与脚本一致；`docs/ARCHITECTURE.md` 定义孔名/单位/原点/旋转/轨道。 |
| M1 Agent 可生成有效设计 | 完成 | Schema、目录、孔位生成、放置变换、导通图、事务、CLI；两个示例 + 9 个反例由 `scripts/build-examples.ts` 通过同一事务引擎生成；CLI 测试验证失败不落盘。 |
| M2 面包板画布 | 完成 | 渲染、缩放平移、落孔吸附、放置/旋转/复制/删除、多选、锁定、孔占用/遮挡区分；两板拼接（栅格对齐）；四次旋转回到原映射（单元 + e2e）。 |
| M3 手工连线与校验闭环 | 完成 | 接线、拐点编辑、颜色/线号、导通图、规则面板点击定位、网络高亮；e2e 制造电源对地短路 → 修复 → 警告消失；撤销/重做。 |
| M4 保存、导出、DSL、接线向导 | 完成 | localStorage 自动保存 + 上一项目恢复 + 保存失败提示；JSON 导入/导出往返哈希一致；SVG/PNG 导出含图例与徽标、无裁切（CLI 测试检查 viewBox）；DSL 面板非法草稿不污染画布；接线向导勾选状态刷新后保留。 |
| M5 示例、性能、体验 | 完成 | 两个可编辑示例 + 压力样本；元件 JSON 导入（内嵌到设计）；性能数据见下。 |
| M6 开源准备与发布 | 进行中 | README/CONTRIBUTING/SECURITY/LICENSE/THIRD_PARTY_NOTICES/issue 模板/CI/Pages 工作流已写；发布结果见文末。 |
| M7 多选自动排线 | 完成 | UI 多选主板 + 外设；电源/GND/I²C/GPIO 角色规划；杜邦线允许重叠/跨越；硬质跳线正交且不共用线段；CLI `autowire`；整批事务撤销；单元 + e2e。 |
| M8 外观编辑器 | 完成 | 属性面板“编辑外观绘图…”：`render` 图元自动分部件（重叠合并、大面积独立、`g` 标签持久化），点选/框选、拖动、方向键微调、复制、删除、旋转、坐标输入、撤销重做；实物照片底图（缩放/旋转/透明度/拖动对位）；保存即 `add_definition` 内嵌到项目（可撤销），或导出定义 JSON 写回元件库。绘图相关的两个测试原先把真实 N16R8 绘图当夹具（钉死描边色 `#e4e7e3`、`bootLabel - 4` 这样的索引偏移、部件 id `s1`），一次正常重画就会失败——现在几何合并规则改用测试自备的合成绘图断言，真实定义只断言与画法无关的不变量（每个图元恰属一组、底板独立且 large、打标签后重分组一致），e2e 则从定义文件里挑一个「含多个图元的部件」再点。N16R8 板按用户照片二次校准（状态灯间距、稳压器朝向、Type-C 位置，补 16 颗小电容电阻）。 |
| M7.3 自动排线 I²C 地址冲突处理 | 完成 | 目录新增 `electrical.i2c.controllers/mappable`、主控 `config.i2c_buses`；规则引擎按每条总线检查地址；规划器接线前分配总线，冲突时开第 2 条总线（自动选最近空闲 GPIO）或改用 `address_options` 中的地址，均列为待审核；三块同地址 SSD1315 场景从 1 个 error 变为 0 error + 2 待审核。 |
| M7.2 自动排线全局优化 | 完成 | 目标函数明确（走线长度 + 拐弯 2 mm + 杜邦线 6 mm + 每线 3 mm）；信号/I²C 网络 ≤ 7 节点穷举生成树（Prüfer + 孔位容量），电源/GND 枚举电源轨段子集并跨网络联合选段，走线顺序拆线重排 + 孔位重选；只在不劣于贪心时采用并报告 `plan.optimization`。环境节点示例目标值 845.7 → 818.8 mm（−3.2%，穷举），压力样本 1722.7 → 1682.2 mm（−2.4%，启发式），桌面示例贪心已最优；耗时 15–400 ms。 |
| M7.1 自动排线选点与配电改进 | 完成 | 端点对用画布同一避障路由器试算取最短实际路径（串接 I²C 自动走空闲排）；电源/GND 按“馈线 + 跨段/跨板桥线”最短路配电，馈线从最近的等效主板引脚组出发（830 板馈线 < 15 mm，此前跨整板 109 mm）；`auto` 线材按长度/绕路选择硬质跳线或杜邦线；其他主控只共地；`signal_pins` 提前校验。桌面示例：11 根线（8 硬质 + 3 杜邦），0 error；4 板/20 模块压力样本规划 < 0.6 s。 |
| 实机（Web Serial） | 完成 | `docs/PHASE5_RFC.md` §5 建议里唯一动手做的一条：把 WebSerial 从「阶段 5 的一条路线」改名成一个诚实的独立模式，顶栏因此有了第三个开关。做的是「看串口 + 硬复位」，**烧录没有做**，所以 esptool-js 没有引入，入口 chunk 703,674 → 746,493 B（闸门 900,000 B）。三条自我约束写进了代码和测试：不实现也不注册 `SimulationBackend`（十个方法里 `pause`/`step`/`reset`/`setSpeed`/`setBreakpoints` 它都给不了，真实 MCU 没有虚拟时钟可停）；不喂器件模型（真板的引脚接的是桌上的真元件，画布上的 OLED 与传感器不在那个电路里）；离开「实机」就挂断（看不见的串口是惊吓，重连只要一次点击）。分层是为了可测：Web Serial 只出现在 `browser-serial.ts` 一个文件里，行解码、缓冲、状态机都不需要浏览器就能测。e2e 用 `addInitScript` 注入一个假的 `navigator.serial`，于是跑的是真的适配器和真的解码器，而不是它们的替身——半行「I (410) app: hel」必须先不显示、补上「lo\n」才显示，钉住的是流式解码而不是截图。复位那条断言钉的是时序本身：RTS 拉低、DTR 保持不动——反过来会把芯片带进下载模式而不是正常启动；走原生 USB 口的板子根本没有这条电路，所以按钮叫「复位」而文案说的是拉低 EN 约 100 ms。不支持的浏览器写明原因（Firefox/Safari 没有 Web Serial，http 非安全上下文也不行），而不是给一个按了没反应的按钮。单测 15，e2e 5。 |
| 阶段 5 真实固件后端 | 已调查，谢绝 | 计划 §14.2 要求先出 RFC 与技术验证再决定，`docs/PHASE5_RFC.md` 是那份 RFC。三条路线各由一个 agent 联网取证、再各由一个 agent 攻击、最后一个裁判排序。判定：ESP-IDF QEMU 否决——`hw/gpio/esp32_gpio.c` 全文 109 行且写处理函数字面为空、`hw/xtensa/esp32s3.c` 实例化 26 个外设而无 I²C（两条我亲自拉源码复核），本项目赖以存在的两个外设恰好都不存在，且是政策不是积压；另有 GPL-2.0 与本仓库 MIT 的冲突（第三条我复核了本仓库这一半）。指令集模拟器是唯一能满足接口的，且不引入第二个 wasm、宿主器件层完全不用改，但地基是一个 2.5 周新、1 star、文档自陈「没有 I²C 模型」的仓库。WebSerial 给不了 `SimulationBackend` 十个方法中的五个，重新归类为独立功能。结论是**结构性**的：浏览器里没有东西能编译 Xtensa，「真实固件」只能是别人编译好的 `.bin`，而有 `.bin` 的人有工具链和板子。已调查并谢绝是计划本来就允许的结局。 |
| S4.6 BMP390 气压传感器 | 完成 | 阶段 4 最后一个器件，也是**唯一一个「器件诚实」与「真实库算得对」是两件事**的器件：它报的是原始 ADC 值，要经 21 字节标定块和一个三次多项式才变成气压，敷衍地返回一个方便的数字会让所有真实 BMP3xx 库算出错的压强。所以补偿直接抄自 Bosch 官方 BMP3-Sensor-API 的 `bmp3.c`（`BMP3_FLOAT_COMPENSATION` 分支），寄存器表对照数据手册 BST-BMP390-DS002-07 与 Linux 内核 `bmp280-core.c` 交叉核对——四个 agent 分头把这些原始资料下载到本地，我再自己读原文，不经转述。两个凭记忆必错的点：`par_t1` 与 `par_p5` 是**除以** 0.00390625 和 0.125，即乘 256 和 8，而源码注释写的是「1 / 2^8」「1 / 2^3」，读起来正好相反。滑杆值到原始寄存器值用**二分**而非代数反演：补偿对原始值单调，40 次折半落到 1 LSB 以内，且仿真器与真实库跑的是同一个多项式的两个方向，不必手工维持两份表达式一致。标定块是选定的、不是实测的（没有公开来源给出典型 NVM 值），高阶系数取到量化后为零以保证映射良态。单测 18（驱动 15、整链路 3），e2e 1。最强的一条证据是整链路测试：客体代码**独立**实现同一套多项式，经寄存器接口读回 1013.2 hPa / 25 °C，两个独立实现对上了。**四个对抗性审查 agent 抓到 7 个真错误**（每条都带数据手册或 Bosch 源码行号，两个 lens 独立指向同一组），已全部修正：① `rawFor` 原本反解的是**带钳位**的函数，−40 °C 处落在平段上返回 raw 0，而不带钳位的解码器（数据手册 §8.5、Adafruit、以及我自己整链路测试里的客体代码）读到的是 −40.13 °C——钳位现已拆成独立包装，只在解码侧生效；② 多字节写在 BMP390 上是「地址/数据成对」而非自增（Bosch `interleave_reg_addr`），原实现会让 Adafruit 配置 OSR/ODR/CONFIG 时每个值落错一个寄存器；③ STATUS 的 `cmd_rdy`(0x10) 从未置位，而 `bmp3_init` 调用的 `bmp3_soft_reset` 以它为前提、Linux 驱动会 -EBUSY 探测失败；④ 数据寄存器复位值是 0x800000 而非 0（Table 25）；⑤ 软复位只清了 PWR_CTRL，未恢复 OSR/ODR/CONFIG；⑥ forced 模式永不结束，真实器件转换一次后模式位自清；⑦ EVENT(0x10) 的 `por_detected` 未建模。同时补上转换时长模型（抄自 `bmp3_get_meas_dur`）与 drdy 读后自清。 |
| S4.5 输入录制与回放 | 完成 | 仿真器从 M-S1 起就保证「同样的输入给同样的运行」，但没人手上有「同样的输入」——输入来自人。录制把这条保证变成可用的东西：每一次控件操作由 **Worker** 打上虚拟时刻（主线程的镜像滞后一帧，差一帧的录制回放不出原来那次运行），经 `control-log` 通道排队送出；回放时在 `prepare` 与 `start` 之间装载，由调度器在**原来的时刻**重新投递。录制属于一次运行，开新会话即清空——否则回放会把自己录进去，第二次就翻倍。文件格式 `breadboard-studio/recording@1`，可导出导入；来自另一版设计的录制不拒绝（想验证改动是否修好了问题，正需要拿旧录制去跑），只提示结果可能不同。单测 7（回放 4、文件解析 3），e2e 1。变异验证：把回放改成立即投递后，两条用例失败。 |
| S4.4 网络时间线与断点 | 完成 | 网络监视只能回答「现在是什么」，回答不了「刚才那一下发生了没有」——500 µs 的脉冲在数值表里看不见，画成方波就一目了然。数字网络内核记录每次跳变，经**独立协议通道** `net-trace` 送出（排队而非合并：合并掉一条边就是时间线上的一个洞）。三处都有界：内核缓冲 8192 条、控制器保留 4000 条、面板只画最近 2 秒与最近活动的 8 条网络；任何丢弃都会计数并在面板上写明，绝不静默抹平。断点做成「点一下网络名，下次跳变即暂停」——检查放在 drain 时而不是内核里，因为在 settle 中途暂停会留下解到一半的网络；代价是停在含该边的那个 tick 末尾而非边上，而时间线仍按真实时刻画出那条边。新增诊断码 `breakpoint_hit`(info)。单测 10（时间线几何 6、内核边记录 2、断点 2），e2e 2。变异验证：内核不记录跳变后时间线 e2e 失败。 |
| S4.3 LTR390 与 SEN66 | 完成 | 补齐 `environment_node` 里另外两个传感器，顺带把三种 I²C 寻址形态都建了模：SHT4x 是 8 位命令字，**LTR390 是寄存器指针**（写地址、读时自增），**SEN66 是 16 位命令字 + 每个数据字带 CRC**。照真实库写的程序因此能跑通，而不是只对某一种形状有效。LTR390 的原始计数经数据手册的增益与分辨率表换算，改 `ALS_UVS_GAIN` 读数真的会变；只有被选中的模式（ALS/UVS）会转换，忘记设 MAIN_CTRL bit3 就读 UVS 得到 0。SEN66 需要先 `0x0021` 启动并等一个测量周期，提前读 NACK 并提示轮询 `0x0202`；九个量各自独立滑杆，不强加 PM1≤PM2.5≤PM4≤PM10 的物理约束——仿真器只如实报告滑杆的值。命令字、寄存器表与换算全部标注未经实物验证并由测试钉死。单测 15（LTR390 8、SEN66 7）。**BMP390 仍无驱动**：博世的补偿需要 21 字节标定系数与多项式反解，凭记忆写一个 20 系数的多项式风险远高于写错一个命令码，需要对着数据手册单独做。 |
| S4.2 SHT4x 传感器与属性滑杆 | 完成 | `sensor.sht4x@1` 是**第一个真正应答 I²C 读事务的器件**——此前全仓唯一的 `onI2cRead` 是 OLED 的，永远返回 null，所以读通路有单测和时序公式却从没被任何器件走通过。驱动如实建模「测量要花时间」：命令立即 ACK，但结果在转换完成（高精度约 8.3 ms）之前不存在，提前读就 NACK 并给出可操作提示，忘记 `sleep` 的程序在这里失败的方式和在实物上一样。命令编码与换算公式取自数据手册、**未经实物验证**（与 SSD1315 同状态），由测试钉死：CRC-8（poly 0x31 / init 0xFF）、`T = -45 + 175·S/65535`、`RH = -6 + 125·S/65535`。滑杆范围进目录（`SimulationControlDef.range`：min/max/step/default/unit），因为 −40…125 °C 是这个元件的属性，界面不该知道 SHT4x 是什么；滑杆渲染在右侧仿真面板而不是画布，沿用 M-S2 跳过画布 slider 的判断。单测 11（驱动 8、整链路 3），e2e 1。变异验证：让滑杆不发送控件事件后 e2e 失败。 |
| S4.1 电阻导通与分立 LED | 完成 | `GPIO → 电阻 → LED → GND` 这本入门书第一个电路，此前 **LED 永远不亮**：串联电阻在导通图里是断路。定义新增 `conduction`（二端无源件），连通性分成 `full`（电流能过去）与 `direct`（同一电学节点）两层，`output.led@1` 驱动按「阳极高、阴极低」判亮灭，颜色取自 `params.color`（快照新增 `SimDeviceSpec.params`）。顺带补上一个真实缺口：接地网络在数字域里原本是 `Z`，现在解析为强 0——接地网络从不被驱动，所以从来不会被 settle，值一直悬空；这正是 LED 阴极接地却不亮的原因。单测 17（导通与阻值解析 9、LED 驱动 5、整链路集成 3）。实测：夹具加一个 220 Ω 与一只绿色 LED，程序驱动 GPIO5 时 `intensity` 为 1，拉低即灭，`full.connected('mcu.GPIO5','led1.A')` 为真而 `direct.connected` 为假。 |
| S3 仿真阶段 3：I²C 与 OLED（M-S3） | 完成 | 控制器级 I²C：按 `sdaNet`/`sclNet` 字符串对匹配，事务按位数计时（写 2 字节 29 bit = 73 µs @400 kHz）、到期时原子提交，同一控制器 FIFO 串行；地址无人应答 / 双器件同址 / 未供电 / SDA-SCL 接反各自给出不同措辞的 warning，并按 `code|componentId|address|sda|scl` 去重（20 ms 一轮的 loop 只留一条）。`display.ssd1315@1`：GDDRAM 128×8、三种寻址模式的指针推进、列/页窗口、反显、对比度、全屏点亮、显示开关、Co=1 单字节帧、未知命令只警告一次且不丢状态；断电即黑但保留画面。像素走 `visualBus` 旁路直接进 `<canvas>`，8 KB 帧不进 zustand。夹具程序升级为完整演示：触摸 → 屏幕 Ready/Touched + 板载 RGB 蓝转绿。单测 30（I²C 12、SSD1315 11、集成 7），e2e 3。四反例实测互不相同：断 SDA → `i2c_bus_unavailable`（两端网络键双双消失，`begin()` 就失败，比 NACK 更能指出问题）；地址改 0x3D → `i2c_nack`，只点名 oled、两条网络完好；断 VCC → `device_unpowered` + 屏幕 `enabled:false`；GPIO4 设成输出推高 → `digital_contention`。四种情况下会话全部保持 running。变异验证：`EMPTY_PIXELS` 换成 8192 字节后「帧不进 store」用例失败。**修正（2026-09-09）**：未上电时 ESP32 驱动原本报「程序不会执行」，而程序其实照常执行（串口有输出就是证据），只是引脚全 Z、I²C 拒绝，任何输出都不生效——错误的文案让用户以为是自己接线错了。已改为如实描述，并加了一条会话级用例钉住「未上电时程序仍然执行且虚拟时间推进」。 |
| S2 仿真阶段 2：物理输入（M-S2） | 完成 | TTP223 触摸驱动（瞬时/自锁、反相、断电高阻）；画布叠加层做 feature 命中与 pointer capture，点触摸区/BOOT/RST 不改变选择、不修改设计；控件按目录 `simulation.controls` 路由而非别名表；运行中拖动被前置门控冻结。浏览器实测：真实 mouse.down 点触摸区 → TOUCH_IO 由 0 变 1 → 程序打印；剪掉 w11 后同一次按压程序再也收不到 1，只报 `floating_input`(warning) 且会话继续；按 RST 后 setup 再执行一次；idle 时叠加层完全卸载、`elementFromPoint` 归还面包板、拖动照常；深递归被如实报告且页面仍可交互。 |
| U2 外观绘图写回元件库 | 完成 | 外观编辑器新增「写回元件库」，经 Vite 开发插件 `POST /__bbs/definition` 覆盖 `packages/catalog/src/definitions/<id>.json`，改一次所有项目生效（此前只有内嵌进单个文档、或手动搬下载文件两条路，前者会被 `replace_design` 静默丢弃）。校验分工：浏览器跑 `validateComponentDefinition`（内置目录启动时的同一校验器）不合格就不发送；服务端只认已存在的 `<id>.json`，且 `kind`/`id`/`version` 必须与原文件一致——不能新建、不能穿目录、不能张冠李戴。`apply: 'serve'` + `import.meta.env.DEV` 保证只在本地开发存在，`check:dist` 把 `__bbs/definition` 列入禁字串。另外 DSL 应用若丢弃当前仍在用的内嵌绘图，会明确提示并告知可撤销。单测 12（写回 6 + 丢弃判定 6），e2e 1（按钮存在 + 端点对未知 id/穿目录/错 version/错 kind/非对象/GET 六种情况分别拒绝且不写文件）。真机实测：把 `BBS_DEFINITIONS_DIR` 指向临时目录，编辑器里挪动一个部件 1 mm 后写回，文件里正好 4 个图元的 x 各 +1000 µm、327 个图元补齐 `g` 标签、身份不变，仓库文件未被触碰。 |
| U3 复制粘贴 | 完成 | `⌘C`/`⌘X`/`⌘V` 复制元件与面包板：粘贴落到指针所在的孔位，走的是拖放同一套吸附规则（`snapPlacement` 从 `Canvas.tsx` 抽成 `apps/web/src/placement.ts`，两边共用，不会各自漂移）。多选粘贴保持相对位置；添加与吸附合成一次事务，所以一次撤销就干净。缓冲区写进 localStorage，可跨项目粘贴，目标项目缺少型号时明确报错而不是丢一半。导线不随复制（选中导线复制会说明原因，`⌘X` 也不会删它）。页面有文本选区时 `⌘C`/`⌘X` 让给浏览器。单测 6（吸附幂等、按孔位往返、远离面板保持板外、粘贴走位、leadPin 旋转后取左上），e2e 5（粘到指针孔位、一次撤销、重载后仍可粘、剪切先入缓冲再删、仿真模式无剪贴板）。变异验证：让粘贴忽略指针位置后第 ①④ 例失败。 |
| U1 搭建 / 仿真模式切换 | 完成 | 顶栏右上角一个开关把界面分成两半：搭建给出元件库、工具、撤销/重做、DSL 与接线向导；仿真给出运行控件、诊断、串口与网络监视，并撤走一切会改电路的入口。从仿真切回搭建会结束会话（订阅 `mode` 的是仿真侧，`store.ts` 仍不认识运行时）；画布拖动同时受模式与会话双重门控，编辑类快捷键在仿真下静默失效；旧「搭建模式」更名为右栏「接线向导」，画布高亮随该标签开合。e2e 4 例（各模式只显示自己的控件、运行中切回搭建后同一条被拒的编辑生效、仿真里无会话也拖不动且 Delete 无效、接线向导仍在搭建侧且高亮跟随）。变异验证：去掉模式门控后第 3 例失败，去掉停止订阅后第 2 例失败。 |
| S1 仿真阶段 1：程序真的会跑（M-S1） | 完成 | 用户代码在 QuickJS 沙箱里真实执行：调度器与虚拟时钟、数字网络（0/1/Z/X）与电源域、sucrase 转译、Worker 会话、ESP32-S3 的 GPIO/BOOT/RST/板载 RGB 驱动、倍速、启动前电气检查与「仅调试：强制启动」。浏览器实测：夹具程序每 500 ms 翻转板载 RGB、串口打出 `ready`、虚拟时间单调推进；死循环在 5 ms 时间片内被中断并定位到源码行；`await new Promise(()=>{})` 报死锁而不是预算超限；10× 倍速的虚拟时间增量大于 1× 的 3 倍；短路设计默认拒启、勾选强制启动后可运行且红色横幅常驻。主包 712,981 B（阈值 900,000），wasm 恰好 1 个走独立 chunk。I²C 与 OLED 见 S3。 |
| S0 仿真阶段 0：schema 1.1 与最小外壳 | 完成 | schema 1.1（`programs`/`simulation`）与 `1.0 → 1.1` 无损迁移（单元测试：迁移后哈希不变、旧示例照常载入）；`add_program`/`update_program`/`remove_program`/`set_simulation_config` 事务与删除级联；`program_target_*`/`simulation_program_missing` 规则；`packages/sim` 状态机与控制器（无后端时 `run` → `faulted` + `runtime_unavailable`）；目录 `simulation` 绑定通过 schema 校验；CLI `programs`/`program import`/`program export`（新建 + 激活 → validate 0 error、导出字节一致、更新 revision +1、错误目标退出 1、缺 `--target` 退出 2、revision 冲突退出 3、1.0 文件导入后写出 1.1 且哈希与 1.1 导入一致）；Web“仿真”标签保存代码可撤销、随项目导入导出。**不执行代码。** |

## 测试结果（本机）

| 套件 | 结果 |
| --- | --- |
| `pnpm typecheck` | 7 个包全部通过（新增 `packages/sim`） |
| `pnpm test`（Vitest） | 479 通过：BMP390 驱动 15、BMP390 整链路 3、录制回放 4、录制文件 3、时间线几何 6、边记录与断点 4、LTR390 驱动 8、SEN6x 驱动 7、SHT4x 驱动 8、传感器整链路 3、电阻导通与阻值解析 9、LED 驱动 5、LED 整链路 3、I²C 总线 12、SSD1315 驱动 11、触摸显示集成 7、放置吸附与剪贴板几何 6、TTP223 驱动 14、叠加层几何 21、定义写回 6、内嵌绘图丢弃判定 6、Studio TS 沙箱 36（隔离、模块白名单、确定性、预算、行号定位）、数字网络 28、仿真控制器 18（含启动前检查与强制启动）、Worker 会话 17（RGB 闪烁竖切、暂停/复位/RST、死循环、死锁、确定性重放）、自动排线 16、ESP32-S3 驱动 14、编译 14、CLI 13、放置 12、程序与仿真配置 12、泵 12、电源域 12、规则 11、事务/往返 11、调度器 9、outbox 8、面包板导通 7、驱动注册表 7、导线/避障 6、颜色 6、schema 迁移 5、导入边界 5、目录 4、内嵌定义 4、诊断码注册表 3、倍速 3、触摸显示示例 3、外观分组/变换 2、哈希 2（含 I²C 冲突三例）、CLI 13（含 autowire dry-run/写入/require-all/optimize 与 program import/export）、放置 12、程序与仿真配置 12（事务、级联、规则、迁移往返）、规则 11、事务/往返 11、仿真控制器 10（状态机 4 + 会话/快照/诊断）、面包板导通 7、导线/避障 6、schema 迁移 5、目录 4（含仿真绑定一致性）、内嵌定义 4、触摸显示示例 3、外观分组/变换 2、哈希 2 |
| `pnpm test:e2e`（Playwright） | 49 通过：BMP390 滑杆 1、录制回放 1、时间线与断点 2、传感器滑杆 1、M-S3 I²C 与 OLED 3、复制粘贴 5、外观写回元件库 1（按钮只在本地开发出现；端点对未知 id/穿目录/错 version/错 kind/非对象/GET 六种情况分别拒绝、一个字节都不写）、模式切换 4（各模式只显示自己的控件、运行中切回搭建后被拒的编辑生效、仿真里无会话也不可拖动且 Delete 无效、接线向导高亮随标签开合）、M-S2 验收 5（触摸经导线到达程序、断线后收不到且会话继续、RST 重跑 setup、idle 时叠加层不干扰编辑、深递归不打死 Worker）、M-S1 验收 5（板载 RGB 由程序驱动并交替、暂停冻结虚拟时间、复位归零、死循环被中断且定位到源码行且主线程仍可交互、死锁与预算超限可区分、10× 倍速的虚拟时间增量 > 1× 的 3 倍、短路拒启与强制启动横幅）、外观编辑器（选中部件、微调、复制、删除、保存内嵌、撤销）、关键流程、拼板、库与详细开发板、自动排线（默认自动线材：硬质 + 杜邦混合、馈线/桥线、意图闭合、整批撤销；再强制全杜邦线）、仿真外壳 5（程序列表与保存/撤销、无后端时如实报 `runtime_unavailable`、拓扑改动使快照过期、导出导入与 1.0 迁移后不自动运行、无程序时 `program_missing`、倍速不打断会话且草稿不跨项目） |
| `pnpm build` + `pnpm check:dist` | 在 `/breadboard-studio/` 子路径下加载示例、24 根线、5 个网络、0 控制台错误；并真的启动一次仿真（`nowUs` 推进到 100,000）走通「子路径 + 生产构建 + Worker + wasm」。主 chunk 740,450 B（阈值 900,000）、不含 quickjs/sucrase 字样、`.wasm` 恰好 1 个 |
| 浏览器实测（1400×800 / 1200×560） | 代码抽屉拖到极限时画布仍保留 ≥120 px、中间栏不溢出；运行→故障后改倍速仍是“故障”且写入文件，不产生过期诊断 |

计划第 10 节的十类关键测试与用例对应：1 `board-connectivity`；2 `two boards`；3 `wires`；4 `placement`（旋转/脱格/同孔）；5 `rules`（短路、未知→needs_review）；6 `I2C rules`；7 `file round trip`；8 `transactions`+`cli`（原子、冲突、撤销、UI/CLI 一致）；9 `e2e`；10 `cli export`（viewBox 内无裁切、徽标与标签存在）。

## 性能（`pnpm perf` / `pnpm perf:browser`）

4 块 400 孔板、20 个模块、100 根线、1600 孔、约 3000 个场景节点：

| 指标 | 数值 |
| --- | --- |
| 核心分析（模型 + 导通 + 规则），Node 26 | ≈ 9.6 ms |
| 场景构建 / SVG 导出 | 0.6 ms / 6.6 ms |
| 单次 `move_component` 事务 | ≈ 20.4 ms |
| 浏览器内分析（HeadlessChrome 153） | ≈ 8.7 ms |
| 拖动 2 s（120 次指针移动）帧时间 | 平均 16.6 ms，p95 16.8 ms，最大 19.3 ms（≈ 60 fps） |

没有为指标提前重构；SVG 在目标规模下未见卡顿。

## 已知限制

- 内置定义几何均为 `approximate` 或 `unknown`，未经实测；DevKit 排距、转接板针序、SEN66 峰值电流、电源模块能力都需要用户填写。
- 供电只做资料数值比较；不检查 I²C 上拉、GPIO 复用、总线电容；不做三维碰撞分析。
- 830 孔板轨道断点按 MB-102 常见做法建模，实物可能连续。
- 自动排线依赖目录的引脚角色，不会猜测无源/未知引脚；“全局优化”是在给定目标函数下的搜索：网络超过 7 个节点或电源轨超过 10 段时改用启发式，走线顺序是局部搜索，报告中的 `exhaustive` 会如实标注；密集线束仍可能需要手动整理拐点。
- 只支持 2.54 mm 栅格与 90° 倍数旋转。
- 数据仅存于浏览器 localStorage；无云端同步。
- 仿真做到阶段 4（阶段 5 已调查并谢绝，见上）：GPIO、板载 RGB、BOOT/RST、TTP223 触摸、控制器级 I²C 与 SSD1315 OLED、分立 LED 与电阻、SHT4x / LTR390 / SEN66 / BMP390、时间线断点、录制回放都可用。没有的是：真实固件（浏览器编译不了 Xtensa）、WS2812 时序、I²C 逐位波形与上拉/时钟拉伸、电流发热与器件损坏、类型检查。
- 「实机」只连串口看输出并拉一下 EN，**不能烧录固件**，也不与仿真共享任何东西：真板的引脚接的是桌上的真元件。需要 Chromium 系浏览器与安全上下文；走原生 USB 口的板子没有自动复位电路，复位按钮对它无效。
- Studio TS 只做语法转译，不做类型检查：写 `.ts` 不等于有类型保护，类型错误要到运行期才暴露为 `program_runtime_error`。
- 死循环保护不可恢复：预算耗尽会终止当前 loop 并进入故障态，只能复位，不能续跑。

## 下一步

第三期「可执行仿真」（规格书阶段 1–4）已全部交付，见 [`SIMULATOR_RUNTIME_PLAN.md`](SIMULATOR_RUNTIME_PLAN.md)；阶段 5「真实固件后端」按计划先做 RFC，结论是已调查并谢绝（[`PHASE5_RFC.md`](PHASE5_RFC.md)），其中站得住的那部分已作为「实机」模式交付。仿真方向接下来若要继续，值得做的是烧录固件（esptool-js，惰性加载）与更多器件建模。其它方向：实测校验并把核实过的定义改为 `verified`；终端条无轨面包板型号；I²C 上拉与 GPIO 复用规则；密集线束整理；可选 MCP 封装。

## 发布记录

- 仓库：https://github.com/7dul2/breadboard-studio （公开，MIT）
- 演示：https://7dul2.github.io/breadboard-studio/ ，由 `Deploy GitHub Pages` 工作流自动部署；`scripts/check-live.mjs` 对线上站点做过冒烟检查（载入示例、通过引擎制造并检出电源对地短路、撤销、导出 SVG，0 控制台错误）。
- 首次推送后 CI 因 `pnpm/action-setup` 与 `packageManager` 重复指定版本失败，已在 `eb6e758` 修复。
- 版本标签与 Release 见仓库 Releases 页面（v0.1.0）。
